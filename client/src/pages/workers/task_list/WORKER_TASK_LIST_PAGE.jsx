import { observer } from 'mobx-react-lite';
import React from 'react';


const WORKER_TASK_LIST_PAGE = () => {
 
    return (
        <div>
            WORKER_TASK_LIST_PAGE
        </div>
    )
};

export default observer(WORKER_TASK_LIST_PAGE)
