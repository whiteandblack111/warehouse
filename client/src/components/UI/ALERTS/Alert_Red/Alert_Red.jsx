import { observer } from "mobx-react-lite"
import styles from "./alert_Red.module.css"




const Alert_Red = (props) => {

   
        return (
            <div
                className={`${styles.container}`}
            >
          </div>
        )
    


}

export default observer(Alert_Red);